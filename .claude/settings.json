{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "timeout": 120,
            "statusMessage": "版本号 +1，重建 IPK，上传 Release...",
            "command": "jq -r '.tool_input.command' | grep -q 'git commit' && bash /Users/lxf/Code/OpenWrt_RDP_Auth/release.sh 2>&1 | tail -5 | jq -Rs '{systemMessage: .}' || true"
          }
        ]
      }
    ]
  }
}
